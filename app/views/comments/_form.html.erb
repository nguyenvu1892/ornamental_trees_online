<%= form_for([@product, Comment.new], remote: true) do |f| %>
  <%= f.text_field :content, class: "form-control", :placeholder => (t ".add") %>
  <%= f.hidden_field :product_id, value: @product.id %>
  <%= f.submit %>
<% end %>
